# =========================================================
# SIGAP — Environment Variables Khusus Docker Container
# =========================================================

# Koneksi ke database MySQL container (nama host container: 'db')
DATABASE_URL="mysql://sigap_user:sigap_password_2026@db:3306/sigap_db"

# Kunci JWT untuk sesi administrator
JWT_SECRET="sigap_pg_trangkil_super_secret_key_2026"

# URL Publik Aplikasi
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Pengaturan Port & Node
PORT=3000
HOSTNAME="0.0.0.0"
NODE_ENV="production"
